pipeline{
   agent any 
   tools{
    maven 'M2_HOME'
   }
   stages{
    stage("maven"){
        steps{ 
        sh 'mvn clean validate compile install test package'
        }
    }
    stage("artifatory"){
        steps{
             sh 'curl -uadmin:password -T \
            target/bio*.jar \
            "http://100.25.130.174:8081/artifactory/Geolocation/Geosolution/bio_${BUILD_ID}.jar"'
        }
    }
   }
}